Sipmle test app with the following packages: redux, react-router, redux-thunk, redux-form, connected-react-router and reselect on top of the https://github.com/facebook/create-react-app starter repo.

To run the app :

```sh
cd test-app
npm install
npm start
```